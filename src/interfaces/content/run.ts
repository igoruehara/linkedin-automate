// Borda do content script (placeholder). A Extração real (DOM → Perfil), via Anti-Corruption Layer,
// entra na feature 0003. Aqui, nada é lido nem enviado — walking skeleton.
export function run(): void {
  // no-op
}
