// Service worker (MV3) — walking skeleton.
// Sem lógica de orquestração ainda; os casos de uso entram nas features seguintes.
// `defineBackground` é auto-importado pelo WXT.
export default defineBackground(() => {
  // no-op
});
